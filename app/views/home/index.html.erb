<h1>Byagu</h1>
<ul>
<p><% @product.each do |product| %></p>
	<%= product.name %>

   <% product.photos.each_with_index do |photo, i| %>
   		<% if i == 0%>
	<%= link_to (image_tag photo.data.url(:medium)), product_path(product.id) %>
	    <% end %>
	<% end %>

<% end %>

</ul>

